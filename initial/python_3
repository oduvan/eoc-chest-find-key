def find_key(a):
    # your code here
    return None


if __name__ == '__main__':
    print("Example:")
    print(find_key('A simple string'))

    # These "asserts" are used for self-checking and not for an auto-testing
    assert find_key('A simple string') == False
    assert find_key('this string has a key') == True
    assert find_key('this string has a better KEY') == True
    print("Coding complete? Click 'Check' to earn cool rewards!")
